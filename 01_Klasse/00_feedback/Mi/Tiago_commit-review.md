# Commit Review — 2026-06-03

**Commits reviewed:** 5
**Expected tasks:** dom, eventhandler, document.getElementById, calculator
**Grade scale:** AT-1-5

---

## Tiago

**Pull result:** updated
**Branch:** main
**Commits reviewed:** 5 (oldest → newest)

### Commit Timeline

| # | Hash | Message | Date | Files changed |
|---|------|---------|------|---------------|
| 1 | `5692ffa` | Mercedes_Website recommit | — | — |
| 2 | `81d3fa9` | Mercedes website update | — | — |
| 3 | `ee046f1` | Mercedes Website recommit | — | — |
| 4 | `f565a41` | Mini_Rechner unfinished | 2026-05-20 | 6 |
| 5 | `49bafa8` | Mercedes Benz Website Done | 2026-06-01 | 2 |

### Git Review

| Commit | Message quality | Scope | Hygiene | Notes |
|--------|----------------|-------|---------|-------|
| `5692ffa` | ❌ Poor | ✅ Atomic | ✅ Clean | "recommit" pattern — should describe actual change |
| `81d3fa9` | ⚠️ Vague | ✅ Atomic | ✅ Clean | — |
| `ee046f1` | ❌ Poor | ✅ Atomic | ✅ Clean | Duplicate "recommit" message |
| `f565a41` | ✅ Good | ⚠️ Mixed | ✅ Clean | Honest "unfinished" label; bundles Mercedes + JS files |
| `49bafa8` | ✅ Good | ✅ Atomic | ✅ Clean | 198 insertions — large Mercedes update |

### Code Review

**Commit `f565a41` — Mini_Rechner unfinished**

- What was done: Added `03_html&js/JS_HTML/index.html` with two inputs and an "add" button, plus `index.js` with two event handlers (`document.getElementById`, `addEventListener`). Also created a `Test/` page.
- Issues / Risks:
  - **Critical bug**: `index.js` reads input values using `.nodeValue` instead of `.value` (`01_Webbasics/03_html&js/JS_HTML/index.js` line 7–8). `.nodeValue` on an input element always returns `null` — the add operation will output `NaN`.
  - Only the addition operation is implemented — subtract, multiply, divide are missing.
  - An `alert("Test")` fires immediately on page load (`index.js` line 1) — this is debugging code that should not be committed.
  - `Test/index.js` uses `document.querySelector(".btn")` but does nothing with the result.
- Suggestions: Replace `.nodeValue` with `.value`. Remove the `alert("Test")`. Complete the remaining three operations.

### Task Fulfillment

| Expected item | Status | Notes |
|---------------|--------|-------|
| DOM manipulation | ✅ Done | `innerHTML = "Yessir"` in button handler |
| Event handler | ✅ Done | `addEventListener("click", ...)` used |
| `document.getElementById` | ✅ Done | Used in `index.js` |
| Calculator (+, -, *, /) | ⚠️ Partial | Only add button exists, and it is broken due to `.nodeValue` bug |

### Grade

| Criterion | Score |
|-----------|-------|
| Code Quality (40%) | 45 |
| Task Fulfillment (30%) | 50 |
| Git Discipline (20%) | 55 |
| Effort (10%) | 65 |
| **Weighted Score** | **51** |

**Grade: 4 (Genügend)** — Tiago hat die richtigen DOM-Konzepte eingesetzt, aber ein kritischer `.nodeValue`-Bug verhindert jede Berechnung. Der Rechner hat nur eine Operation und ist laut Commit selbst als "unfinished" markiert. Die "recommit"-Nachrichten müssen durch beschreibende Texte ersetzt werden.

---

*Report generated by GitHub Copilot on 2026-06-03*
