# Commit Review — 2026-06-03

**Commits reviewed:** 5
**Expected tasks:** dom, eventhandler, document.getElementById, calculator
**Grade scale:** AT-1-5

---

## Jakob

**Pull result:** no changes
**Branch:** main
**Commits reviewed:** 5 (oldest → newest)

### Commit Timeline

| # | Hash | Message | Date | Files changed |
|---|------|---------|------|---------------|
| 1 | `839367b` | css | — | — |
| 2 | `881f4d9` | 1 Video fertig | — | — |
| 3 | `6902c8c` | 2 Video fertig | 2026-05-20 | 4 |
| 4 | `186f284` | 3 Video angefangen | 2026-05-20 | 2 |
| 5 | `14f0325` | nur kurz zeit wegen superkahoot | 2026-05-28 | 3 |

### Git Review

| Commit | Message quality | Scope | Hygiene | Notes |
|--------|----------------|-------|---------|-------|
| `839367b` | ❌ Poor | ✅ Atomic | ✅ Clean | "css" is too vague |
| `881f4d9` | ⚠️ OK | ✅ Atomic | ✅ Clean | — |
| `6902c8c` | ⚠️ OK | ✅ Atomic | ✅ Clean | — |
| `186f284` | ⚠️ OK | ✅ Atomic | ✅ Clean | — |
| `14f0325` | ❌ Poor | ✅ Atomic | ✅ Clean | Unprofessional message — do not reference classroom events |

### Code Review

**Commit `6902c8c` — 2 Video fertig**

- What was done: Created `01_Zugriff_JS_Html_2Vid/` with `index.html` and `script.js`. The script correctly uses `document.getElementById` to get both a button and a content div, then uses `addEventListener("click", ...)` to update the `innerHTML`.
- Issues / Risks: `<hl>` tag used instead of `<h1>` in `index.html` line 11 — this is an HTML typo that will render as an unknown element (no heading displayed).
- Suggestions: Fix `<hl>` → `<h1>`.

**Commit `14f0325` — nur kurz zeit wegen superkahoot**

- What was done: Minor HTML formatting cleanup (lowercase `<!doctype html>`, self-closing meta tags). Added `script.js` with an event listener for the "Add" button.
- Issues / Risks:
  - `script.js` reads input with `.nodeValue` instead of `.value` (`03_Web/Übungen_CSS/01_Zugriff_JS_Html_3Vid/script.js` line 2). `nodeValue` on an input element returns `null` — the calculation will never work.
  - Still the `<hl>` typo from the previous commit is present.
  - No result display element — the `console.log` only logs to DevTools.
  - Commit message is unprofessional.
- Suggestions: Use `.value` to read input fields. Add a result `<div>` to display the output on screen.

### Task Fulfillment

| Expected item | Status | Notes |
|---------------|--------|-------|
| DOM manipulation | ✅ Done | `innerHTML` updated in `6902c8c` |
| Event handler | ✅ Done | `addEventListener("click", ...)` used |
| `document.getElementById` | ✅ Done | Used in both JS files |
| Calculator (+, -, *, /) | ❌ Missing | Add button exists but `.nodeValue` bug means it never calculates |

### Grade

| Criterion | Score |
|-----------|-------|
| Code Quality (40%) | 50 |
| Task Fulfillment (30%) | 60 |
| Git Discipline (20%) | 50 |
| Effort (10%) | 55 |
| **Weighted Score** | **54** |

**Grade: 4 (Genügend)** — Jakob hat DOM-Manipulation und Event-Handler korrekt umgesetzt, aber ein kritischer Bug (`.nodeValue` statt `.value`) verhindert die Funktion des Rechners. Der Rechner selbst fehlt noch. Commit-Nachrichten müssen professioneller formuliert werden.

---

*Report generated by GitHub Copilot on 2026-06-03*
