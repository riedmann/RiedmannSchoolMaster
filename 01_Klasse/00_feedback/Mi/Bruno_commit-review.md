# Commit Review — 2026-06-03

**Commits reviewed:** 5
**Expected tasks:** dom, eventhandler, document.getElementById, calculator
**Grade scale:** AT-1-5

---

## Bruno

**Pull result:** updated
**Branch:** main
**Commits reviewed:** 5 (oldest → newest)

### Commit Timeline

| # | Hash | Message | Date | Files changed |
|---|------|---------|------|---------------|
| 1 | `a74ce9a` | Mercedes Benz Website heading done | 2026-05-xx | — |
| 2 | `71d0b63` | mercedes Benz | 2026-05-xx | — |
| 3 | `786073b` | javascript übung rechner | 2026-05-20 | 12 |
| 4 | `cd47cfe` | Rechner mit css gestaltet | 2026-05-28 | 5 |
| 5 | `0c72664` | rechner + - * / gemacht | 2026-06-02 | 6 |

### Git Review

| Commit | Message quality | Scope | Hygiene | Notes |
|--------|----------------|-------|---------|-------|
| `a74ce9a` | ⚠️ Vague | ✅ Atomic | ✅ Clean | — |
| `71d0b63` | ⚠️ Vague | ✅ Atomic | ✅ Clean | — |
| `786073b` | ✅ Good | ⚠️ Mixed | ✅ Clean | Also reorganizes CSS files alongside new JS |
| `cd47cfe` | ✅ Good | ✅ Atomic | ✅ Clean | — |
| `0c72664` | ✅ Good | ⚠️ Mixed | ✅ Clean | Calculator + Zufallsgenerator in one commit |

### Code Review

**Commit `786073b` — javascript übung rechner**

- What was done: First JS/DOM exercise. An HTML page with two inputs and an "add" button. Uses `document.getElementById`, `addEventListener`, input validation with `isNaN`, `parseFloat`.
- Issues / Risks: A "hello" heading (`<h1>hello</h1>`) and a test button left in the file (`01_Web/Web-Script/01.html`). `console.log(value1)` is debugging output that should be removed. No `placeholder` text on inputs (added later).
- Suggestions: Clean up debug artifacts before committing.

**Commit `cd47cfe` — Rechner mit css gestaltet**

- What was done: Moved files into a proper `Rechner/` subfolder and added CSS styling.
- Issues / Risks: The JS file moved without changes — no functional improvement in this commit.
- Suggestions: Good practice to separate layout changes from feature changes.

**Commit `0c72664` — rechner + - * / gemacht**

- What was done: Extended the calculator with subtract (`add1`), multiply (`add2`), and divide (`add3`) buttons. All four operations implemented using `addEventListener`. Input validation (`isNaN`) present on all handlers.
- Issues / Risks:
  - Button IDs `add1`, `add2`, `add3` are confusing — they should be named `subtract`, `multiply`, `divide` (`01_Web/Web-Script/Rechner/01.js`).
  - No division-by-zero check for the divide operation — `Infinity` will be displayed.
  - `console.log(value1)` debug output is duplicated in every handler.
  - Files committed without newline at end of file.
  - `Zufallsgenerator/02.html` and empty CSS/JS files for that task were bundled into this commit — should be a separate commit.
- Suggestions: Use descriptive IDs, add a `if (parseFloat(value2) === 0)` guard for division, remove `console.log` statements.

### Task Fulfillment

| Expected item | Status | Notes |
|---------------|--------|-------|
| DOM manipulation | ✅ Done | `element.innerHTML`, `result.innerHTML` used |
| Event handler | ✅ Done | `addEventListener("click", ...)` on all buttons |
| `document.getElementById` | ✅ Done | Used consistently throughout |
| Calculator (+, -, *, /) | ✅ Done | All four operations implemented |

### Grade

| Criterion | Score |
|-----------|-------|
| Code Quality (40%) | 75 |
| Task Fulfillment (30%) | 85 |
| Git Discipline (20%) | 70 |
| Effort (10%) | 80 |
| **Weighted Score** | **78** |

**Grade: 2 (Gut)** — Bruno hat alle vier Rechenoperationen korrekt implementiert und die wichtigsten DOM-Konzepte angewendet. Kleinere Schwächen bei Bezeichnungen, fehlendem Division-durch-null-Check und Debug-Ausgaben verhindern eine Bestnote.

---

*Report generated by GitHub Copilot on 2026-06-03*
