# Commit Review — 2026-06-03

**Commits reviewed:** 5
**Expected tasks:** dom, eventhandler, document.getElementById, calculator
**Grade scale:** AT-1-5

---

## Selim

**Pull result:** no changes
**Branch:** main
**Commits reviewed:** 5 (oldest → newest)

### Commit Timeline

| # | Hash | Message | Date | Files changed |
|---|------|---------|------|---------------|
| 1 | `87f184f` | Task 03_Aufgabe | — | — |
| 2 | `5cd5529` | Task 06 Aufgabe | — | — |
| 3 | `8877d08` | commit | — | — |
| 4 | `56bfcff` | 01_Aufgabe.html | — | — |
| 5 | `95ae144` | task done | 2026-03-04 | 9 |

### Git Review

| Commit | Message quality | Scope | Hygiene | Notes |
|--------|----------------|-------|---------|-------|
| `87f184f` | ⚠️ OK | ✅ Atomic | ✅ Clean | — |
| `5cd5529` | ⚠️ OK | ✅ Atomic | ✅ Clean | — |
| `8877d08` | ❌ Poor | ✅ Atomic | ✅ Clean | "commit" is not a meaningful message |
| `56bfcff` | ❌ Poor | ✅ Atomic | ❌ Hygiene | Filename used as commit message; file extension typo |
| `95ae144` | ⚠️ Vague | ❌ Not atomic | ❌ Hygiene | 9 unrelated files bundled; file renamed to `script.jss` (double s) |

### Code Review

**Commit `95ae144` — task done**

- What was done: Added several HTML/CSS files. `WEB/index.html` has a button with an `onclick="zeigeNachricht()"` attribute, but the function `zeigeNachricht()` is never defined — the script file is missing or not linked.
- Issues / Risks:
  - `WEB/index.html` links `style.css` but no such file exists in `WEB/` — broken link.
  - `Web.HTML/Aufgabe.html` has a broken `<html` opening tag (missing closing `>`).
  - A file was renamed from `WEB/index.htlm` to `HTML aufgaben/lenny/script.jss` — both the original and new names contain extension typos (`.htlm`, `.jss`).
  - Uses inline `onclick` attribute instead of `addEventListener` — not the expected pattern.
- Suggestions: Fix the broken HTML tag. Use `addEventListener` instead of `onclick`. Define and link the `zeigeNachricht` function.

### Task Fulfillment

| Expected item | Status | Notes |
|---------------|--------|-------|
| DOM manipulation | ⚠️ Partial | Button with onclick exists, but handler function is undefined |
| Event handler | ⚠️ Partial | Inline `onclick` used, not `addEventListener` |
| `document.getElementById` | ❌ Missing | Not used |
| Calculator (+, -, *, /) | ❌ Missing | Not built |

### Grade

| Criterion | Score |
|-----------|-------|
| Code Quality (40%) | 35 |
| Task Fulfillment (30%) | 25 |
| Git Discipline (20%) | 40 |
| Effort (10%) | 40 |
| **Weighted Score** | **35** |

**Grade: 5 (Nicht Genügend)** — Selim hat die grundlegenden DOM-Konzepte nicht korrekt umgesetzt: der Event-Handler ist als `onclick`-Attribut definiert (statt `addEventListener`), die zugehörige Funktion fehlt, und der Rechner wurde nicht gebaut. Mehrere Dateien haben fehlerhafte Extensions oder HTML-Fehler.

---

*Report generated by GitHub Copilot on 2026-06-03*
