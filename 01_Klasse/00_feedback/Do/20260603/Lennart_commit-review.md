# Commit Review — Lennart

**Commits reviewed:** 5
**Expected tasks:** dom eventhandler document.getelementbyid calculator
**Grade scale:** AT-1-5

---

**Pull result:** no changes
**Branch:** main
**Commits reviewed:** 5 (oldest → newest)

### Commit Timeline

| # | Hash | Message | Date | Files changed |
|---|------|---------|------|---------------|
| 1 | `d2fcb35` | hoppala | — | — |
| 2 | `edc6de9` | Mercedes | — | — |
| 3 | `ac0e2d6` | MERCEDES | 2026-04-27 | 2 |
| 4 | `5f8832d` | Finished mercedes Website | 2026-04-30 | 3 |
| 5 | `f64b36e` | Calculator | 2026-05-28 | 2 |

### Git Review

| Commit | Message quality | Scope | Hygiene | Notes |
|--------|----------------|-------|---------|-------|
| `d2fcb35` | ❌ Unprofessional ("hoppala") | — | — | — |
| `edc6de9` | ⚠️ Too generic | — | — | — |
| `ac0e2d6` | ⚠️ All-caps, vague | ⚠️ Mixed | ✅ Clean | Refactors CSS layout |
| `5f8832d` | ✅ Good | ✅ Focused | ⚠️ Binary `.woff2` font file committed | Font file tracked in git |
| `f64b36e` | ✅ Good | ✅ Atomic | ✅ Clean | — |

> ⚠️ **Git Hygiene:** A binary font file (`Mercedes_Top_Font.woff2`, 24 KB) was committed in `5f8832d`. Binary assets should ideally be hosted or excluded via `.gitignore`. Not critical, but worth noting.

### Code Review

**Commits `ac0e2d6` / `5f8832d` — MERCEDES / Finished mercedes Website**

- What was done: Progressive improvement of the Mercedes website — fixed positioning, added proper responsive media queries (`@media (max-width: 1000px)`, `@media (max-width: 700px)`), custom font faces, and a call-to-action button. This is high-quality CSS work for the level.
- Issues / Risks: Minor typo in CSS: `font size: 50px;` (missing hyphen, should be `font-size`) inside the `@media (max-width: 700px)` block. Also `overflow: hidden` is set but this hides the `</body>` tag — actually that's in a different commit.
- Suggestions: Fix the `font size` typo. The overall structure is impressive.

**Commit `f64b36e` — Calculator**

- What was done: `1bWI_Web/Html+js/calculator.html` + `calculator.js` — full 4-operation calculator using `document.getElementById` and inline `onclick` handlers.
- Issues / Risks:
  - Uses `onclick="calculate(...)"` inline instead of `addEventListener` — this works but mixes HTML and JS concerns.
  - No input validation: if both fields are empty, `Number("")` returns `0`, giving misleading results (e.g., `0 + 0 = 0`).
  - Division by zero returns `Infinity` with no error message.
- Suggestions: Add input validation (check for empty/non-numeric values). Handle division by zero. Consider moving the event binding to JS with `addEventListener`.

### Task Fulfillment

| Expected item | Status | Notes |
|---------------|--------|-------|
| `document.getElementById` | ✅ Done | Used in `calculator.js` |
| DOM Eventhandler (`addEventListener`) | ⚠️ Partial | `onclick` used instead of `addEventListener` in `calculator.js` |
| Calculator (mini rechner) | ✅ Done | All 4 operations, clean logic in `calculator.js` |

### Grade

| Criterion | Score |
|-----------|-------|
| Code Quality (40%) | 75 |
| Task Fulfillment (30%) | 65 |
| Git Discipline (20%) | 60 |
| Effort (10%) | 75 |

**Weighted Score: 70** → **Grade: 3**

Strong effort — the calculator is well-structured and the Mercedes site shows advanced CSS knowledge including media queries and custom fonts. The main gap is using `onclick` inline instead of `addEventListener`, which is what the eventhandler exercise specifically targets. Input validation is also missing from the calculator. Commit messages like "hoppala" and "MERCEDES" should be more professional.

---

*Report generated by GitHub Copilot on 2026-06-03*
