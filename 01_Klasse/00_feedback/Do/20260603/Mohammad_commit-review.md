# Commit Review — Mohammad

**Commits reviewed:** 5
**Expected tasks:** dom eventhandler document.getelementbyid calculator
**Grade scale:** AT-1-5

---

**Pull result:** no changes
**Branch:** main
**Commits reviewed:** 5 (oldest → newest)

### Commit Timeline

| # | Hash | Message | Date | Files changed |
|---|------|---------|------|---------------|
| 1 | `e5e01fa` | Video fertig | — | — |
| 2 | `85a145a` | Zusatz letzte aufgaben die boxen nicht geschafft | — | — |
| 3 | `786dcca` | Finish position and box sizing | — | — |
| 4 | `ff21c3c` | Mercedes head ferig | 2026-04-23 | 10 |
| 5 | `d276f65` | Übungen von 1 und Übung 2 | — | 8+ |

### Git Review

| Commit | Message quality | Scope | Hygiene | Notes |
|--------|----------------|-------|---------|-------|
| `e5e01fa` | ⚠️ Vague | — | — | — |
| `85a145a` | ✅ Honest & descriptive | — | — | Mentions what was not completed |
| `786dcca` | ✅ OK | — | — | — |
| `ff21c3c` | ✅ Good | ⚠️ Many files in one commit | ⚠️ Binary assets + font committed | Background PNG ~1.7MB |
| `d276f65` | ⚠️ Unclear ("Übungen von 1 und Übung 2") | ⚠️ Too large (8+ files) | ✅ Clean | Mixes multiple exercises |

> ⚠️ **Git Hygiene:** `02_Web/Mercedes_Benz/assets/background.png` (~1.7 MB) is committed in `ff21c3c`. Large binary files should be avoided in git — consider using an external URL for the image.

### Code Review

**Commit `ff21c3c` — Mercedes head ferig**

- What was done: Added a detailed Mercedes header HTML with custom CSS (`stylebenz.css`), including SVG icons and proper flexbox layout. Good structural work.
- Issues / Risks: Large binary image committed (1.7 MB). Typo in commit message: "ferig" → "fertig".
- Suggestions: Link the background image via URL instead of committing the binary. Fix the commit message typo.

**Commit `d276f65` — Übungen von 1 und Übung 2**

- What was done: Committed three exercises at once: eventhandler (`02.eventhandler.js`), mini calculator (`03.minirechner.js` + `03.minrechner.html`), and a bonus random number generator (`04.Zufallsgenerator.js/html/css`).
- Issues / Risks:
  1. **Critical HTML bug in `03.minrechner.html`:** `<button>id="button">Add</button>` — the `id` attribute is written outside the opening tag. This means the button has no `id`, so `document.getElementById("button")` returns `null` and the calculator won't work at all.
  2. **Wrong script reference:** `<script src="index.js" defer>` instead of `03.minirechner.js` — the correct JS file is never loaded.
  3. The commit bundles three different exercises into one — harder to review and trace history.
- Suggestions: Fix the HTML: `<button id="button">Add</button>`. Update the script `src` to `03.minirechner.js`. Split exercises into separate commits.

**Bonus: `04.Zufallsgenerator`**

- What was done: A well-structured random number generator with input validation (empty check, min > max check) and clear output.
- Issues / Risks: None — this is good extra work.
- Suggestions: Excellent bonus exercise. The logic is clean and validates edge cases properly.

### Task Fulfillment

| Expected item | Status | Notes |
|---------------|--------|-------|
| `document.getElementById` | ✅ Done | `02.eventhandler.js` — correct |
| DOM Eventhandler (`addEventListener`) | ✅ Done | `02.eventhandler.js` — correct |
| Calculator (mini rechner) | ❌ Broken | `03.minrechner.html` has two critical bugs: wrong HTML syntax for button id, wrong script src |
| Bonus: Zufallsgenerator | ✅ Done | Well implemented |

### Grade

| Criterion | Score |
|-----------|-------|
| Code Quality (40%) | 52 |
| Task Fulfillment (30%) | 55 |
| Git Discipline (20%) | 50 |
| Effort (10%) | 65 |

**Weighted Score: 54** → **Grade: 4**

The eventhandler task is correctly implemented and the bonus Zufallsgenerator shows real understanding. However, the calculator is broken due to two bugs in the HTML file — the button `id` is placed outside the tag and the wrong script is referenced. These prevent the feature from running entirely. Large binary committed in git. Fixing the two HTML bugs would significantly improve the grade.

---

*Report generated by GitHub Copilot on 2026-06-03*
