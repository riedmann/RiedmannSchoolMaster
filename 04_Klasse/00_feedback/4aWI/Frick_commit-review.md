# Commit Review — 2026-05-29

**Commits reviewed:** 5  
**Expected tasks:** strategy factory singleton observer pattern  
**Grade scale:** AT-1-5

---

## Frick

**Pull result:** updated  
**Branch:** main  
**Commits reviewed:** 5 (oldest → newest)

### Commit Timeline

| # | Hash | Message | Date | Files changed |
|---|------|---------|------|---------------|
| 1 | `581b4a6` | Implement payment strategy pattern: add PaymentContext and various payment methods | 2026-05-27 | 5 |
| 2 | `9b13f7e` | Add .gitignore file to exclude unnecessary files and directories | 2026-05-27 | 1 |
| 3 | `13d1be2` | Refactor payment module: move PaymentContext and payment strategies to Onlineshop directory | 2026-05-27 | few |
| 4 | `1173051` | Refactor MyGame initialization: replace hardcoded actors with ActorFactory for dynamic actor creation | 2026-05-29 | 2 |
| 5 | `5fe6da7` | Add GameStandings class and integrate scoring system into MyGame and Circle | 2026-05-29 | 4 |

### Git Review

| Commit | Message quality | Scope | Hygiene | Notes |
|--------|----------------|-------|---------|-------|
| `581b4a6` | ✅ Excellent | ✅ Atomic | ✅ Clean | Detailed, clear intent |
| `9b13f7e` | ✅ Good | ✅ Atomic | ✅ Clean | Dedicated hygiene commit |
| `13d1be2` | ✅ Excellent | ✅ Atomic | ✅ Clean | Clear refactor scope |
| `1173051` | ✅ Excellent | ✅ Atomic | ✅ Clean | Professional-level message |
| `5fe6da7` | ✅ Excellent | ✅ Atomic | ✅ Clean | Precise scope description |

**Git Discipline:** Excellent. Commit messages are professional, atomic, and descriptive. A dedicated `.gitignore` commit shows good housekeeping awareness.

### Code Review

**Commit `581b4a6` — Implement payment strategy pattern**

- What was done: `PaymentStrategy` interface with `pay(amount)`. Three concrete implementations: `CreditCardPayment`, `CryptoPayment`, `PaypalPayment`. `PaymentContext` holds a strategy reference and delegates `pay()`.
- Issues/Risks: All three `pay()` method bodies are `// TODO: implement` — the structure is correct but no actual logic is present (`Bank/payment/CreditCardPayment.ts` line 5).
- Suggestions: Add at least a `console.log` to demonstrate the strategy is called. The pattern structure itself is clean and correct.

**Commit `1173051` — Replace hardcoded actors with ActorFactory**

- What was done: `ActorFactory` class with `createCircle()`, `createRectangle()`, `createTree()` factory methods and `createRandomActors(n)`. Randomized positions, sizes, colors, and movement strategies. `MyGame.init()` now delegates to `factory.createRandomActors(10)`.
- Issues/Risks: `ActorFactory` is not a Singleton itself — a new one is instantiated in `MyGame.init()` each time. This is fine but note that `GameStandings` uses `getInstance()` (added next commit), so the Singleton is coming.
- Suggestions: Clean and well-structured factory.

**Commit `5fe6da7` — Add GameStandings class and Singleton**

- What was done: `GameStandings` implements Singleton (`getInstance()`, private constructor, static `instance`). It's also an `Actor` that renders a score overlay. `Circle` receives a `standings?` reference and calls `standings.increaseScore(10)` when `x > 400` (first time only via `hasScored` guard).
- Issues/Risks: `hasScored` flag is per-circle, but the score is global — once a circle crosses 400 px it can never score again even if it comes back. This may be intentional but limits gameplay. `GameStandings.instance` is never reset between games.
- Observer pattern: `GameFramework.ts` contains a full `addObserver/removeObserver/notifyObservers` implementation, and `Circle` and `Rectangle` implement `Observer` with `inform()`. `MyGame` uses `addObserver(actor)` in `init()`. Observer pattern is wired correctly.

### Task Fulfillment

| Expected item | Status | Notes |
|---------------|--------|-------|
| Strategy Pattern | ✅ Done | `MoveStrategy` (game) + `PaymentStrategy` (bank) — two real-world contexts |
| Factory Pattern | ✅ Done | `ActorFactory` with typed creation methods |
| Singleton Pattern | ✅ Done | `GameStandings.getInstance()` — correct lazy Singleton |
| Observer Pattern | ✅ Done | `Observer` interface, `Game.addObserver/notifyObservers`, actors implement `Observer` |

### Grade

| Criterion | Score |
|-----------|-------|
| Code Quality (40%) | 82 |
| Task Fulfillment (30%) | 87 |
| Git Discipline (20%) | 90 |
| Effort (10%) | 88 |

**Weighted Score: 85** → **Grade: 2 (Gut)**

Aaron hat alle vier Design Patterns sauber und eigenständig implementiert — mit dem Bonus, Strategy in zwei verschiedenen Kontexten (Spielerbewegung und Zahlungsabwicklung) angewendet zu haben. Commit-Nachrichten sind vorbildlich. Kleinere Abzüge für die noch leeren `pay()`-Implementierungen und ein paar Randfälle bei der Spiellogik.

---

*Report generated by GitHub Copilot on 2026-05-29*
