# Commit Review — 2026-05-29

**Commits reviewed:** 5  
**Expected tasks:** strategy factory singleton observer pattern  
**Grade scale:** AT-1-5

---

## Hagen

**Pull result:** no changes  
**Branch:** master  
**Commits reviewed:** 5 (oldest → newest)

### Commit Timeline

| # | Hash | Message | Date | Files changed |
|---|------|---------|------|---------------|
| 1 | `436154a` | Typescript_Datentypen DONE | 2025-10-03 | — |
| 2 | `2f611ce` | Traffic Light | 2026-01-16 | — |
| 3 | `93a3c1e` | Implement 2D Game Framework with Game and GameObject interfaces, and example game implementation | 2026-05-20 | 14 |
| 4 | `68963f8` | Implement 2D Game Framework with Game and GameObject interfaces, example game implementation, and HTML setup | 2026-05-20 | 14 |
| 5 | `35c7e65` | Refactor actor classes to use AbstractActor and implement MoveStrategy for movement handling | 2026-05-27 | 8 |

### Git Review

| Commit | Message quality | Scope | Hygiene | Notes |
|--------|----------------|-------|---------|-------|
| `436154a` | ✅ Good | ✅ Atomic | ✅ Clean | Old exercise |
| `2f611ce` | ⚠️ Vague | ⚠️ OK | ✅ Clean | Old traffic light |
| `93a3c1e` | ✅ Excellent | ⚠️ Large | ✅ Clean | Duplicate of `68963f8` with HTML! |
| `68963f8` | ✅ Excellent | ⚠️ Large | ✅ Clean | Both commits have nearly identical messages |
| `35c7e65` | ✅ Excellent | ✅ Atomic | ✅ Clean | Good, specific message |

**Git Issue:** Commits `93a3c1e` and `68963f8` both have nearly identical commit messages and appear to implement the same thing. This looks like a duplicate/amended commit that wasn't squashed, or two attempts at the same change. Consider using `git rebase -i` to clean up history.

### Code Review

**Commit `68963f8` — Implement 2D Game Framework**

- What was done: Full initial game framework with `GameFramework.ts`, `Actor` interface, `Circle`, `Rectangle`, `SuperCircle`, `MyGame`.
- Issues/Risks: `MyGame.update()` calls `actor.move(deltaTime)` but the `Actor` interface has `move(delta)` — inconsistent naming. Hardcoded actor creation.
- Suggestions: Good starting point.

**Commit `35c7e65` — Refactor actor classes to use AbstractActor and implement MoveStrategy**

- What was done: `MoveStrategy` interface defined with `move(actor: Actor): void`. Two concrete strategies: `MoveRight` and `MoveLeft` (both in same file `MoveStrategy.ts`). `AbstractActor` base class holds a `MoveStrategy` and calls `this.movement.move(this)` in `update()`. `Circle` extends `AbstractActor`. `Homer` class added as a fun extra actor.
- Issues/Risks:
  - **Not integrated:** `MyGame.ts` was NOT updated — it still calls `actor.move(deltaTime)` and instantiates actors without any strategy. The `Circle` constructor now requires a `MoveStrategy` but `MyGame` creates `new Circle(...)` without one. This means the refactored code would not compile correctly against the existing `MyGame.ts`.
  - `Circle.ctx` is stored as a constructor parameter — tight coupling, the canvas context should come from `render()`, not the constructor.
  - `update()` in `AbstractActor` has no `deltaTime` parameter (`update(): void`) while the game loop typically passes delta time. `MoveRight.speed = 2` is frame-count-based rather than time-based — movement will be different at different frame rates.
  - Two separate `Movement.ts` and `MoveStrategy.ts` files were created — this creates confusion. `Movement.ts` defines old-style `direction: "left" | "right"` interfaces that are now superseded.
- Suggestions: Update `MyGame.ts` to use the new constructors. Remove the old `Movement.ts` interfaces. Make `update()` accept `deltaTime` and scale movement accordingly.

### Task Fulfillment

| Expected item | Status | Notes |
|---------------|--------|-------|
| Strategy Pattern | ⚠️ Partial | `MoveStrategy` interface + two strategies defined but **not integrated** into `MyGame.ts` |
| Factory Pattern | ❌ Missing | Actors still created directly in `MyGame.init()` |
| Singleton Pattern | ❌ Missing | Not implemented |
| Observer Pattern | ❌ Missing | Not implemented |

### Grade

| Criterion | Score |
|-----------|-------|
| Code Quality (40%) | 50 |
| Task Fulfillment (30%) | 18 |
| Git Discipline (20%) | 60 |
| Effort (10%) | 48 |

**Weighted Score: 42** → **Grade: 5 (Nicht Genügend)**

Luca hat die richtige Idee für das Strategy Pattern und setzt die Klassen korrekt auf — aber die Implementierung wurde nicht zu Ende geführt: `MyGame.ts` verwendet die neuen Klassen nicht, und der Code würde daher nicht kompilieren. Factory, Singleton und Observer fehlen ganz. Wichtigster nächster Schritt: `MyGame.ts` mit den neuen Strategien verdrahten und `update()` auf zeitbasierte Bewegung umstellen.

---

*Report generated by GitHub Copilot on 2026-05-29*
