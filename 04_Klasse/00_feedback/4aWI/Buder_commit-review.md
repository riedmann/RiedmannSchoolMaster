# Commit Review — 2026-05-29

**Commits reviewed:** 3 (only 3 commits exist)  
**Expected tasks:** strategy factory singleton observer pattern  
**Grade scale:** AT-1-5

---

## Buder

**Pull result:** no changes  
**Branch:** main  
**Commits reviewed:** 3 (oldest → newest)

### Commit Timeline

| # | Hash | Message | Date | Files changed |
|---|------|---------|------|---------------|
| 1 | `59ae5d9` | init | 2026-05-20 | few |
| 2 | `c2dd828` | setup tsc | 2026-05-20 | few |
| 3 | `a159875` | finished game | 2026-05-20 | ~30 |

### Git Review

| Commit | Message quality | Scope | Hygiene | Notes |
|--------|----------------|-------|---------|-------|
| `59ae5d9` | ⚠️ Too vague | ⚠️ Setup | ✅ OK | "init" adds nothing specific |
| `c2dd828` | ⚠️ Vague | ⚠️ Setup | ✅ OK | "setup tsc" minimal info |
| `a159875` | ❌ Too vague | ❌ Huge | ✅ Clean | Entire implementation in one commit, "finished game" is not descriptive |

**Git Discipline Issue:** All meaningful work was committed in a single giant commit (`a159875`). This defeats the purpose of version control — no way to see incremental progress. Commit messages should describe *what* was changed, not just the state. Also, compiled `.js` files are committed alongside `.ts` source.

### Code Review

**Commit `a159875` — finished game**

- What was done: Full implementation in one commit — `ActorFactory`, `GameFramework`, `MyGame`, abstract `AbstractActor`, concrete actors (`Circle`, `Rectangle`, `Homer`, `Tree`), movement strategies (`MoveStrategy`, `RightMovement`, `LeftMovement`), `Observer` interface, `GameStandings` Singleton.

**Strategy Pattern** (`movements/MoveStrategy.ts`, `RightMovement.ts`, `LeftMovement.ts`):
- `MoveStrategy` interface is correct. `RightMovement` and `LeftMovement` implement it.
- Actors receive a strategy via constructor injection — clean design.

**Factory Pattern** (`ActorFactory.ts`):
- `ActorFactory` creates `Rectangle`, `Circle`, `Homer`, `Tree` actors with random movement and dimensions.
- `createRandomActor()` dispatches to private factory methods — correct Factory pattern usage.
- Issue: `factory["createRandomMovement"]()` in `MyGame.ts` calls a private method via bracket notation (lines 24–27). This is a code smell and bypasses TypeScript's access modifiers. Should be refactored to a public method if needed externally.

**Singleton Pattern** (`actors/GameStandings.ts`):
- `GameStandings.createInstance()` implements Singleton correctly (private constructor, static instance, lazy initialization).
- Minor: conventional naming is `getInstance()` rather than `createInstance()`.

**Observer Pattern** (`observer/Observer.ts`, `MyGame.ts`):
- `Observer` interface with `inform(event, data?)` is present.
- `MyGame` registers observers and calls `notifyObservers("click", ...)` on mouse click.
- Issue: Actors themselves don't react meaningfully to the `inform()` call — `Circle` and `Rectangle` have no visible `Observer` implementation shown. The observer pattern wiring is present but the reaction logic is incomplete.

### Task Fulfillment

| Expected item | Status | Notes |
|---------------|--------|-------|
| Strategy Pattern | ✅ Done | `MoveStrategy` interface + `RightMovement`, `LeftMovement` |
| Factory Pattern | ✅ Done | `ActorFactory` with typed factory methods |
| Singleton Pattern | ✅ Done | `GameStandings.createInstance()` — naming could be `getInstance()` |
| Observer Pattern | ⚠️ Partial | Interface + registration present; observer reaction logic incomplete |

### Grade

| Criterion | Score |
|-----------|-------|
| Code Quality (40%) | 62 |
| Task Fulfillment (30%) | 78 |
| Git Discipline (20%) | 40 |
| Effort (10%) | 50 |

**Weighted Score: 62** → **Grade: 4 (Genügend)**

Alle vier Design Patterns sind im Code vorhanden, was eine gute Leistung ist. Der Code ist strukturell solide, enthält aber einige Probleme (Private-Methoden-Hack, unvollständige Observer-Reaktion). Der größte Kritikpunkt ist die Git-Disziplin: die gesamte Implementierung in einem einzigen Commit einzuchecken zeigt kein inkrementelles Arbeiten. Commit-Nachrichten müssen beschreibender sein.

---

*Report generated by GitHub Copilot on 2026-05-29*
